// /dashboard — overview of all three services (CLAUDE.md §11; FR-DASH-01).
// Phase 1 stub; KPI row + snapshot cards built in Phase 8.
export default function DashboardPage() {
  return (
    <main className="p-6 space-y-6">
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <p className="text-gray-600">Implemented in Phase 8 (server-first; live tenant data via the API).</p>
    </main>
  );
}
